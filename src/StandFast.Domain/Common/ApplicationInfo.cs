namespace StandFast.Domain.Common;

/// <summary>The application's own name, in the one assembly every layer references, so the shell title, the Data Protection application name, and the backup file all agree.</summary>
public static class ApplicationInfo
{
    public const string Name = "StandFast";
}
